#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../devkit/src/devkit.js" "$@"
  ret=$?
else 
  sh  "$basedir/../devkit/src/devkit.js" "$@"
  ret=$?
fi
exit $ret
